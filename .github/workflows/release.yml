name: Release

# Publishes @cmssy/mcp-server to npm.
# Trigger by pushing a version tag (e.g. `git tag v0.10.0 && git push --tags`)
# or manually via the Actions tab. Requires an npm Automation access token
# (bypasses 2FA) stored as the `NPM_TOKEN` repository secret.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: pnpm

      # On a tag release the tag must match package.json (source of truth).
      # Manual dispatch publishes the current version as-is.
      - name: Verify tag matches package version
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          tag="${GITHUB_REF_NAME#v}"
          name=$(node -p "require('./package.json').name")
          v=$(node -p "require('./package.json').version")
          if [ "$tag" != "$v" ]; then
            echo "::error::tag v$tag does not match $name@$v"
            exit 1
          fi

      - run: pnpm install --frozen-lockfile

      - run: pnpm build

      - run: pnpm typecheck

      # Skips if the current version is already on npm, so re-runs are safe.
      - name: Publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -e
          name=$(node -p "require('./package.json').name")
          version=$(node -p "require('./package.json').version")
          if npm view "$name@$version" version >/dev/null 2>&1; then
            echo "$name@$version already on npm — skipping"
          else
            echo "Publishing $name@$version"
            pnpm publish --access public --no-git-checks
          fi
